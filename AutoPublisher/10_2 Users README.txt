ArcGIS 10.2 users:

Delete the AutoPub.py file, and rename the AutoPub10_2.py file to AutoPub.py.

The scripts are nearly identical.  Currently the only difference is that  the service description will say "Published using ArcGIS 10.2"